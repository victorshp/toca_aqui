<div class="navbar navbar-expand-sm justify-content-between navbar-light navbar-lewagon shadow-sm bg-white rounded navbar transparent navbar-inverse">
  <%= link_to "/", class: "navbar-brand" do %>
  <!-- link_to will redirect to root. -->
      <%= image_tag "toca_aqui-logo.png", alt: "Toca Aqui logo" %>
  <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"  aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "ANÚNCIOS", "/instruments", class: "nav-link" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <%= image_tag "placeholder_avatar.png", class: "avatar dropdown-toggle", style: 'height: 40px; width: 40px;', id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <p class='mx-3'>Logado como: <b><%= current_user.email %></b></p>
            <% if current_user.admin %>
              <p class='mx-3'><strong>CONTA ADMIN</strong></p>
            <% end %>
            <hr class='mx-2'>
            <% unless current_user.admin %>
              <%= link_to "Meu Perfil", "/profile", class: "dropdown-item" %>
            <% end %>
            <%= link_to "Sair da conta", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <ul class="navbar-nav mr-auto">
        </ul>
        <li class="nav-item">
          <%= link_to "ENTRAR", new_user_session_path, class: "btn btn-warning" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
